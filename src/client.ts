/**
 * Re-export of `HuwiyaClient` from `@hitaqnia/core`. The full implementation now
 * lives in the framework-agnostic core package — new code should import from
 * `@hitaqnia/core` directly.
 */
export { HuwiyaClient } from "@hitaqnia/core";
