/**
 * Re-export of `HuwiyaClient` from `@hitaqnia/huwiya-core`. The full implementation now
 * lives in the framework-agnostic core package — new code should import from
 * `@hitaqnia/huwiya-core` directly.
 */
export { HuwiyaClient } from "@hitaqnia/huwiya-core";
